using System;
using System.IO;
using HydrixOS.Tools;
namespace HydrixOS.Installer.User
{
    public static class User
    {
	/// #######################################################################################
	/// This [CLASS] Has been redacted for security in [Hydrix_OS]. 
	/// Public uploads will not contain the source for these types of classes.
	/// You may have to make your own function or remove some parts to allow a successful post.
	/// #######################################################################################
	Delete_This_Line_To_Remove_Error
        public static bool TryLogin(string username, string password, out bool isadmin)
        {
            // [Redacted For Security]
        }
        private static void NewBasicUser(string username, string password)
        {
            // [Redacted For Security]
        }
        private static void NewAdminUser(string username, string password)
        {
            // [Redacted For Security]
        }
        public static void NewUserSetup(bool forceAdmin = false)
        {
            // [Redacted For Security]
        }
    }
}